# lefthook.yml
pre-commit:
  parallel: true
  commands:
    check:
      root: mdvault/
      run: bun biome check --write --unsafe

    # secrets:
    #   run: gitleaks protect --staged

pre-push:
  commands:
    check:
      root: mdvault/
      run: bun check && bun typecheck
